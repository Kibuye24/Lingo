/**
 * The bare entry point. AuthGate decides where you actually land — login,
 * onboarding, or your language home — and shows a splash meanwhile, so this
 * route renders nothing of its own.
 */
export default function Index() {
  return null;
}
